export type span = HTMLSpanElement | null;
export type div = HTMLDivElement | null;
export type form = HTMLFormElement | null;

export interface DataCard {
    img: string,
    altimg: string, 
    title: string, 
    descr: string, 
    price: number
}